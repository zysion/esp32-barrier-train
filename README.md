whenever want to update or adjust any module. 
  - Create a new branch if it is not exist.
  - just merge the file relative this module to main.(if you update LED_module in brach "LED_module", just only merge LED_module(.h & .cpp) regarderless you fix the other file)
  - Need to commit clearly

The structure folow Free RTOS.
